---
secrets:
  BETTER_AUTH_SECRET: rAfUhO50b96FggXalDRubuvkw3cXxEqb7p3X1iIs0mE=
  # Renders the JSON branch of the service account key. Inert at runtime: with no
  # `config.GOOGLE_GROUP_EMAIL` or `config.GOOGLE_CALENDAR_ID` nothing reads it.
  GOOGLE_SERVICE_ACCOUNT_KEY: |
    {"client_email": "ci@example.iam.gserviceaccount.com", "private_key": "not-a-key"}

postgres:
  auth:
    password: secretPassword
