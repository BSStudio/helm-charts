apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mattermost.fullname" . }}
  labels:
    {{- include "mattermost.labels" . | nindent 4 }}
data:
  {{- include "mattermost.config" . | trim | default "{}" | nindent 2 }}
