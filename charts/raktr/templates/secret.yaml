apiVersion: v1
kind: Secret
metadata:
  name: {{ include "raktr.fullname" . }}
  labels:
    {{- include "raktr.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
type: Opaque
data:
  {{- include "raktr.secrets" . | trim | default "{}" | nindent 2 }}
