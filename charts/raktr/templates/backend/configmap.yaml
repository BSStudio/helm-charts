apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "raktr.fullname" . }}-backend
  labels:
    {{- include "raktr.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
data:
  {{- include "raktr.backendConfig" . | trim | default "{}" | nindent 2 }}
